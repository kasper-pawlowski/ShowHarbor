import { View, Text } from 'react-native';
import React, { useEffect } from 'react';
import { Wrapper } from './ShowView.styles';

const ShowView = () => {
    return (
        <Wrapper>
            <Text>index</Text>
        </Wrapper>
    );
};

export default ShowView;
